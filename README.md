# Sohum Mehta — portfolio

The finished one-page site lives in `index.html` and has no build step. Open it directly or serve this folder with any static server.

Before publishing:

- Add the current resume as `resume.pdf` in this folder.
- Replace the portrait placeholder in the hero.
- Replace or remove the clearly marked `EDIT ME` publication row.
- Review dates, venue status, skill labels, and recognition wording.

The older design explorations remain untouched under `.superpowers/brainstorm/`.
